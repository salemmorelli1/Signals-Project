# Legacy spectral reference benchmark

This directory preserves the first CPU spectral pseudo-posterior benchmark and
its generated evidence. It does not execute joint torus phase or latent-channel
filtering and is retained only for provenance.

The current implementation is `src/signals_project/joint_ssm.py` at the
repository root, with current evidence stored under `data/joint_*`.
